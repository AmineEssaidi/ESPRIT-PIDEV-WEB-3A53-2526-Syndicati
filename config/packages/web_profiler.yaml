when@dev:
    web_profiler:
        toolbar: true
        intercept_redirects: false

    framework:
        profiler:
            enabled: true
            collect_serializer_data: false

when@test:
    framework:
        profiler:
            collect: false
            collect_serializer_data: true
