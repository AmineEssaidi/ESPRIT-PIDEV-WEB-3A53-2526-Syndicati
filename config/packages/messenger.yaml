framework:
    messenger:
        failure_transport: failed

        transports:
            # https://symfony.com/doc/current/messenger.html#transport-configuration
            async:
                dsn: '%env(MESSENGER_TRANSPORT_DSN)%'
                options:
                    use_notify: true
                    check_delayed_interval: 60000
                retry_strategy:
                    max_retries: 3
                    multiplier: 2
            failed: 'doctrine://default?queue_name=failed'
            sync: 'sync://'

        default_bus: messenger.bus.default

        buses:
            messenger.bus.default: []

        routing:
            # Send emails immediately (no worker needed). Use async if you run messenger:consume.
            Symfony\Component\Mailer\Messenger\SendEmailMessage: sync
            Symfony\Component\Notifier\Message\ChatMessage: async
            Symfony\Component\Notifier\Message\SmsMessage: async
            App\Message\Forum\NotifyAnnouncementMessage: async
            App\Message\Forum\NotifyCommentMessage: async
            App\Message\Evenement\NotifyEventCreationMessage: async
            App\Message\Evenement\NotifyParticipationConfirmationMessage: async

            # Route your messages to the transports
            # 'App\Message\YourMessage': async
