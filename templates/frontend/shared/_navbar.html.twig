<!-- Revolutionary Header - Dynamic Island Navbar -->
<script>
// Apply theme, accent, and language from localStorage globally on every page load
(function() {
    try {
        var html = document.documentElement;
        var theme = localStorage.getItem('theme') || 'dark';
        var accentGradient = localStorage.getItem('accent-gradient') || 'linear-gradient(135deg, transparent, transparent, transparent)';
        var accentColor = localStorage.getItem('accent-color') || 'transparent';
        var lang = localStorage.getItem('lang') || 'fr';
        function hexToRgb(hex) {
            var m = /^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(hex);
            return m ? [parseInt(m[1], 16), parseInt(m[2], 16), parseInt(m[3], 16)].join(', ') : '108, 92, 231';
        }
        html.setAttribute('data-theme', theme);
        html.style.setProperty('--main-home-accent-gradient', accentGradient);
        html.style.setProperty('--accent-gradient', accentGradient);
        html.style.setProperty('--main-home-accent-color', accentColor);
        html.style.setProperty('--accent', accentColor);
        html.style.setProperty('--primary', accentColor);
        html.style.setProperty('--main-home-accent-rgb', hexToRgb(accentColor));
        html.style.setProperty('--accent-glow', 'rgba(' + hexToRgb(accentColor) + ', 0.5)');
        var firstColor = accentGradient.match(/#[0-9a-fA-F]{3,8}/)?.[0] || 'transparent';
        html.style.setProperty('--main-home-accent-gradient-border', accentGradient.replace(/\)\s*$/, ', ' + firstColor + ')'));
        html.style.setProperty('--primary-gradient', accentGradient);
        html.setAttribute('lang', lang);
    } catch (e) {}
})();
</script>

<style id="notif-emergency-styles">
    /* High-Priority Visibility Fixes */
    .notif-wrapper {
        position: relative !important;
        display: flex !important;
        align-items: center !important;
        justify-content: center !important;
        width: 46px !important;
        height: 46px !important;
        margin-right: 15px !important;
        z-index: 99999 !important;
    }

    .notif-bell {
        width: 42px !important;
        height: 42px !important;
        background: #1a1a2e !important; /* Forces a solid background to avoid the white box issue */
        background: rgba(var(--main-home-accent-rgb, 26, 26, 46), 0.2) !important;
        border: 1.5px solid rgba(255, 255, 255, 0.2) !important;
        border-radius: 50% !important;
        display: flex !important;
        align-items: center !important;
        justify-content: center !important;
        cursor: pointer !important;
        color: #fff !important;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.4) !important;
        transition: transform 0.2s cubic-bezier(0.175, 0.885, 0.32, 1.275) !important;
    }

    .notif-bell:hover { transform: scale(1.1) !important; }

    .notif-badge {
        position: absolute !important;
        top: -3px !important;
        right: -3px !important;
        background: #ff3b30 !important;
        color: white !important;
        font-size: 10px !important;
        font-weight: 900 !important;
        min-width: 18px !important;
        height: 18px !important;
        border-radius: 10px !important;
        display: flex !important;
        align-items: center !important;
        justify-content: center !important;
        border: 2px solid #000 !important;
        z-index: 100000 !important;
    }

    .notif-hub {
        position: absolute !important;
        top: 55px !important;
        right: -5px !important;
        width: 380px !important;
        max-height: 500px !important;
        background: #0d0d14 !important;
        border: 1px solid rgba(255, 255, 255, 0.1) !important;
        border-radius: 24px !important;
        display: none !important; /* JS will change this to flex */
        flex-direction: column !important;
        z-index: 100001 !important;
        box-shadow: 0 20px 60px rgba(0, 0, 0, 0.8) !important;
        overflow: hidden !important;
    }

    .notif-hub.active { 
        display: flex !important; 
        animation: notif-slide-in 0.3s cubic-bezier(0.16, 1, 0.3, 1) !important;
    }

    @keyframes notif-slide-in {
        from { transform: translateY(15px) scale(0.98); opacity: 0; }
        to { transform: translateY(0) scale(1); opacity: 1; }
    }

    /* Items styles */
    .notif-hub-header { padding: 16px; border-bottom: 1px solid rgba(255,255,255,0.1); display: flex; justify-content: space-between; align-items: center; }
    .notif-hub-header h5 { margin: 0; font-size: 1.1rem; color: #fff; font-weight: 700; }
    .notif-action-btn { background: rgba(255,255,255,0.05); border: none; color: #fff; padding: 6px 12px; border-radius: 8px; font-size: 12px; cursor: pointer; }
    .notif-close-btn { background: none; border: none; color: rgba(255,255,255,0.4); cursor: pointer; font-size: 20px; }
    .notif-hub-body { flex: 1; overflow-y: auto; padding: 10px; min-height: 200px; }
    .notif-item { padding: 12px; border-radius: 12px; display: flex; gap: 12px; cursor: pointer; transition: background 0.2s; margin-bottom: 5px; }
    .notif-item:hover { background: rgba(255,255,255,0.05); }
    .notif-item.unread { background: rgba(var(--main-home-accent-rgb, 108, 92, 231), 0.1); }
    .notif-item-icon { width: 40px; height: 40px; border-radius: 10px; background: rgba(255,255,255,0.05); display: flex; align-items: center; justify-content: center; font-size: 18px; }
    .notif-item-content { flex: 1; }
    .notif-item-title { margin: 0; font-size: 14px; font-weight: 700; color: #fff; }
    .notif-item-text { margin: 4px 0 0 0; font-size: 13px; color: rgba(255,255,255,0.6); }
    .notif-item-time { font-size: 11px; color: rgba(255,255,255,0.3); margin-top: 5px; }
</style>

<nav class="main-home-navbar" style="position:relative; z-index:1000 !important; overflow: visible !important;">
    <!-- Left Side -->
    <div class="main-home-header-left">
        <div class="main-home-search-icon-container">
            <a href="{{ path('main_home') }}" class="main-home-search-icon-btn" title="Home / Search">
                <div class="main-home-search-heart">
                    <div class="main-home-search-heart-shape">
                        <div class="main-home-search-heart-dots">
                            <div class="main-home-search-heart-dot"></div>
                            <div class="main-home-search-heart-dot"></div>
                            <div class="main-home-search-heart-dot"></div>
                            <div class="main-home-search-heart-dot"></div>
                        </div>
                    </div>
                </div>
            </a>
            <div class="main-home-search-expand">
                <input type="text" class="main-home-search-input"  placeholder="Rechercher..." aria-label="Rechercher..."/>{{ 'nav.search'|trans }}</div>
        </div>
    </div>

    <!-- Center -->
    {% if active_tab|default('') not in ['sign_in', 'sign_up'] %}
    <div class="main-home-header-center">
        <div class="main-home-nav-tabs">
            <div class="main-home-nav-tab {{ active_tab|default('') == 'home' ? 'active' : '' }}">
                <span class="main-home-tab-text">
                    <a href="{{ path('main_home') }}"  style="text-decoration: none; color: inherit;">{{ 'nav.home'|trans }}</a>
                </span>
            </div>
            <div class="main-home-nav-tab {{ active_tab|default('') == 'services' ? 'active' : '' }}">
                <span class="main-home-tab-text"><a href="#"  style="text-decoration: none; color: inherit;">{{ 'nav.services'|trans }}</a></span>
                <div class="main-home-tab-dropdown">
                    <a href="{{ path('app_residence_index') }}" class="main-home-dropdown-item" >Residences</a>
                    <a href="{{ path('frontend_forum') }}" class="main-home-dropdown-item" >Forum</a>
                    <a href="{{ path('frontend_syndicat') }}" class="main-home-dropdown-item" >Syndic</a>
                    <a href="{{ path('app_evenement_index') }}" class="main-home-dropdown-item" >Events</a>
                </div>
            </div>
            <div class="main-home-nav-tab {{ active_tab|default('') == 'about' ? 'active' : '' }}">
                <span class="main-home-tab-text"><a href="{{ path('frontend_our_team') }}"  style="text-decoration: none; color: inherit;">{{ 'nav.about'|trans }}</a></span>
                <div class="main-home-tab-dropdown">
                    <a href="{{ path('frontend_our_team') }}" class="main-home-dropdown-item" >Our Team</a>
                    <a href="#" class="main-home-dropdown-item" >Company</a>
                    <a href="{{ path('frontend_contact') }}" class="main-home-dropdown-item" >Contact</a>
                </div>
            </div>
        </div>
    </div>
    {% else %}
    <div class="main-home-header-center"></div>
    {% endif %}

    <!-- Right Side -->
    <div class="main-home-header-right" style="overflow: visible !important;">
        <div class="main-home-ios-theme-toggle">
            <div class="main-home-toggle-container" id="theme-toggle">
                <div class="main-home-toggle-track">
                    <div class="main-home-toggle-thumb">
                        <span class="main-home-toggle-icon main-home-sun-icon">☀️</span>
                        <span class="main-home-toggle-icon main-home-moon-icon">🌙</span>
                    </div>
                </div>
            </div>
        </div>

        {% if app.session.get('is_logged_in') %}
            <div class="notif-wrapper">
                <div class="notif-bell" id="notifBell" title="Notifications">
                    <i class="bx bx-bell" style="font-size: 20px;"></i>
                    <span class="notif-badge" id="notifBadge" style="display: none;">0</span>
                </div>
                
                <div class="notif-hub" id="notifHub">
                    <div class="notif-hub-header">
                        <h5>{{ 'nav.notifications'|trans|default('Notifications') }}</h5>
                        <div style="display:flex; gap:8px; align-items:center;">
                            <button id="notifMarkAllRead" class="notif-action-btn">{{ 'nav.mark_all_read'|trans|default('Tout lu') }}</button>
                            <button id="notifCloseHub" class="notif-close-btn"><i class="bx bx-x"></i></button>
                        </div>
                    </div>
                    <div class="notif-hub-body scrollbar-hide" id="notifScroll"></div>
                </div>
            </div>

            {% set user = app.session.get('user') %}
            <div class="main-home-user-profile-dropdown">
                <div class="main-home-user-avatar-only">
                    <img src="{{ user.avatar|default('https://demos.themeselection.com/sneat-bootstrap-html-admin-template/assets/img/avatars/1.png') }}" alt="Profile" class="main-home-avatar-img"/>
                    <div class="main-home-avatar-status online"></div>
                </div>
                <div class="main-home-dropdown-menu">
                    <div class="main-home-dropdown-item" style="pointer-events: none; border-bottom: 1px solid rgba(255,255,255,0.1); margin-bottom: 5px;">
                        <span style="font-weight: bold; font-size: 0.9rem;">{{ user.first_name|default('User') }} {{ user.last_name|default('') }}</span>
                    </div>
                    <a href="{{ path('app_logout') }}" class="main-home-dropdown-item" style="color: #ff4d6d;">
                        <i class="bx bx-power-off"></i>
                        <span>{{ 'nav.log_out'|trans }}</span>
                    </a>
                </div>
            </div>
        {% else %}
            <div class="main-home-auth-buttons">
                <a href="{{ path('auth_sign_in') }}" class="main-home-btn main-home-btn-auth-secondary {{ active_tab|default('') == 'sign_in' ? 'active' : '' }}">{{ 'nav.sign_in'|trans }}</a>
                <a href="{{ path('user_signup') }}" class="main-home-btn main-home-btn-auth-primary {{ active_tab|default('') == 'sign_up' ? 'active' : '' }}">{{ 'nav.sign_up'|trans }}</a>
            </div>
        {% endif %}
    </div>

    <script>
    document.addEventListener('DOMContentLoaded', function() {
        const bell = document.getElementById('notifBell');
        const hub = document.getElementById('notifHub');
        const closeBtn = document.getElementById('notifCloseHub');

        if (bell && hub) {
            bell.onclick = function(e) {
                e.stopPropagation();
                const isActive = hub.classList.contains('active');
                if (isActive) {
                    hub.classList.remove('active');
                    hub.style.display = 'none';
                } else {
                    hub.style.display = 'flex';
                    hub.classList.add('active');
                }
            };

            if (closeBtn) {
                closeBtn.onclick = function(e) {
                    e.stopPropagation();
                    hub.classList.remove('active');
                    hub.style.display = 'none';
                };
            }

            document.onclick = function(e) {
                if (hub.classList.contains('active') && !hub.contains(e.target) && !bell.contains(e.target)) {
                    hub.classList.remove('active');
                    hub.style.display = 'none';
                }
            };
        }
    });

    // Strip legacy elements
    (function() {
        const legacySelectors = ['.main-home-dynamic-island', '.main-home-notification-circle', '.notif-bell-emoji'];
        legacySelectors.forEach(s => {
            const elements = document.querySelectorAll(s);
            elements.forEach(el => el.remove());
        });
    })();
    </script>
</nav>
