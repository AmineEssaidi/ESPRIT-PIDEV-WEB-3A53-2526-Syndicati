{% extends 'base_symfony.html.twig' %}
{% block title %}Events - Explore What's Happening{% endblock %}

{% block stylesheets %}
    <link href="{{ asset('frontend/css/evenement.css') }}" rel="stylesheet">
    <link href="{{ asset('frontend/css/evenement-form-custom.css') }}" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/themes/dark.css">
    <link href="{{ asset('frontend/css/glass_switcher.css') }}" rel="stylesheet">
{% endblock %}

{% block body %}
<div class="main-home-layout-wrapper">
    <div class="main-home-page">
        {% include 'frontend/shared/_main_navbar.html.twig' with {'active_tab': 'evenement'} %}
        
        <div class="main-home-page-content" style="padding: 2rem 0;">
            <!-- Hero Section -->
            <div class="event-hero-section" style="height:520px;">
                <div class="event-hero-bg-glow"></div>
                <div class="event-bg-glow-secondary"></div>
                
                <div class="container position-relative" style="z-index: 2;">
                    <div class="row align-items-center">
                        <div class="col-lg-8">
                            <span class="event-hero-badge">
                                <i class='bx bxs-zap'></i> Community Experiences
                            </span>
                            <h1 class="event-hero-title">Discover. Connect.<br>Experience.</h1>
                            <p class="event-hero-subtitle">Join exclusive events, workshops, and gatherings designed for our community. Your next great story starts here.</p>
                        </div>
                    </div>
                </div>
            </div>
         <!-- Floating Event Preview -->
                <div id="calendarEventPreview" class="calendar-event-preview">
                    <div class="preview-card">
                        <div class="preview-banner" id="previewBanner"></div>
                        <div class="preview-content">
                            <span class="preview-tag" id="previewType"></span>
                            <h4 class="preview-title" id="previewTitle"></h4>
                            <div class="preview-meta">
                                <i class='bx bx-time-five'></i> <span id="previewDate"></span>
                            </div>
                            <button class="preview-btn" id="previewViewBtn">View Details</button>
                        </div>
                    </div>
                </div>

                <!-- 2. EVENTS GRID (Below) -->
                {% if evenements|length > 0 %}
                    <div style="padding: 0 3rem;">
                        <div class="mb-4 d-flex justify-content-between align-items-center">
                            <span class="event-section-pill">
                                <i class='bx bx-calendar-check'></i> Upcoming Events
                            </span>
                            <div class="event-pagination-info" id="eventPaginationInfo">
                                <span style="color: rgba(255,255,255,0.5); font-size: 0.9rem;">Showing <span id="currentEventRange"></span> of {{ evenements|length }}</span>
                            </div>
                        </div>
                        
                        <div class="event-grid" id="eventGrid">
                            {% for evenement in evenements %}
                                {% include 'frontend/evenement/_card.html.twig' with {'evenement': evenement} %}
                            {% endfor %}
                        </div>
                        
                        <div class="event-pagination-controls" id="eventPaginationControls">
                            <button class="event-pagination-btn" id="prevEventPage" disabled>
                                <i class='bx bx-chevron-left'></i>
                            </button>
                            <div class="event-pagination-pages" id="eventPaginationPages"></div>
                            <button class="event-pagination-btn" id="nextEventPage">
                                <i class='bx bx-chevron-right'></i>
                            </button>
                        </div>
                    </div>
                {% endif %}
            <!-- Event Content -->
            <div id="events" class="pb-5">
                
                <!-- 1. PERMANENT DASHBOARD (Formerly Empty State) -->
                <div class="forum-empty-state mb-5">
                    <!-- Mini Hero Glows -->
                    <div class="event-hero-bg-glow" style="opacity: 0.4;"></div>
                    <div class="event-bg-glow-secondary" style="opacity: 0.3;"></div>
                    
                    <!-- Left Column: Dynamic Messaging -->
                    <div class="empty-state-left">
                        <span class="event-section-pill" style="margin-bottom: 2rem;">
                            <i class='bx bx-calendar-event'></i> Event Dashboard
                        </span>
                        
                        {% if evenements|length > 0 %}
                            <h3 class="event-hero-title" style="font-size: 3rem; margin-bottom: 1.5rem;">Join the Action.<br>{{ evenements|length }} Events Upcoming.</h3>
                            <p class="event-hero-subtitle" style="margin: 0 0 3rem 0; width: 100%; max-width: 500px;">The community is buzzing! Browse the events below and secure your spot before they fill up.</p>
                        {% else %}
                            <h3 class="event-hero-title" style="font-size: 3rem; margin-bottom: 1.5rem;">The Stage is Set...<br>Awaiting Your Pulse.</h3>
                            <p class="event-hero-subtitle" style="margin: 0 0 3rem 0; width: 100%; max-width: 500px;">It looks like there are no events scheduled at the moment. Be the heartbeat of our community and organize something truly legendary.</p>
                        {% endif %}
                        
                        <div class="d-flex gap-3 mt-4">
                                <div class="main-home-made-with-love" style="background: rgba(255,255,255,0.02); padding: 1rem 2rem; border-radius: 20px; border: 1px solid rgba(255,255,255,0.05);">
                                <span style="color: rgba(255,255,255,0.5); font-size: 0.9rem;">Join 500+ members in our next gathering</span>
                                </div>
                        </div>
                    </div>

                    <!-- Right Column: Interactive Dashboard & Actions -->
                    <div id="evenementDashboardSwitcher" class="empty-state-right glass-switcher style-slide">
                        
                        <!-- FACE 1: DASHBOARD MAIN -->
                        <div class="glass-switcher-face glass-switcher-face-main">
                            {% if app.session.get('is_logged_in') %}
                                <div id="btnHostEventDashboard" class="event-host-pill-trigger">
                                    <div style="width: 40px; height: 40px; background: var(--event-gradient); border-radius: 12px; display: flex; align-items: center; justify-content: center; font-size: 1.2rem;">
                                        <i class='bx bx-plus'></i>
                                    </div>
                                    <div class="text-start">
                                        <div style="font-size: 0.95rem; font-weight: 800;">Host an Event</div>
                                        <div style="font-size: 0.75rem; color: rgba(255,255,255,0.4); font-weight: 600;">Start your community journey</div>
                                    </div>
                                    <i class='bx bx-chevron-right' style="margin-left: auto; font-size: 1.5rem; opacity: 0.5;"></i>
                                </div>
                            {% endif %}

                            <!-- Mini Glass Calendar -->
                            <div class="glass-calendar">
                                <div class="calendar-header">
                                    <div class="calendar-month" id="calendarMonthDisplay">{{ "now"|date("F Y") }}</div>
                                    <div class="d-flex gap-2">
                                        <i class='bx bx-chevron-left' id="prevMonth" style="cursor: pointer; opacity: 0.5;"></i>
                                        <i class='bx bx-chevron-right' id="nextMonth" style="cursor: pointer; opacity: 0.5;"></i>
                                    </div>
                                </div>
                                <div class="calendar-grid" id="calendarGrid">
                                    <!-- Days labels -->
                                    <div class="calendar-day-label">Mo</div>
                                    <div class="calendar-day-label">Tu</div>
                                    <div class="calendar-day-label">We</div>
                                    <div class="calendar-day-label">Th</div>
                                    <div class="calendar-day-label">Fr</div>
                                    <div class="calendar-day-label">Sa</div>
                                    <div class="calendar-day-label">Su</div>
                                    
                                    <!-- Days will be injected by JS -->
                                </div>
                                <!-- Category Insight Pills -->
                                <div class="category-tags-list">
                                    <span class="tag-pill">#Workshops</span>
                                    <span class="tag-pill">#Meetups</span>
                                    <span class="tag-pill">#Social</span>
                                    <span class="tag-pill">#Sports</span>
                                    <span class="tag-pill" style="background: rgba(var(--main-home-accent-rgb), 0.1); border-color: rgba(var(--main-home-accent-rgb), 0.2); color: #fff;">+ {{ evenements|length }} Events</span>
                                </div>
                            </div>
                        </div>

                        <!-- FACE 2: HOST EVENT FORM -->
                        <div class="glass-switcher-face glass-switcher-face-extra">
                            <div class="card-header-actions mb-4">
                                <h3 class="m-0" style="font-weight: 850; font-size: 1.8rem;">Organize</h3>
                                <button class="btn-switcher-back" onclick="switchGlassCard('evenementDashboardSwitcher', 'main')">
                                    <i class='bx bx-left-arrow-alt'></i> Back
                                </button>
                            </div>

                            <div class="glass-switcher-content-scroll" style="max-height: 520px; overflow-y: auto; overflow-x: hidden; padding-right: 12px;">
                                {{ form_start(form, {'attr': {'class': 'main-home-auth-form', 'enctype': 'multipart/form-data', 'id': 'createEventFormDashboard', 'data-ajax': 'true'}}) }}
                                    <div class="glass-form-group">
                                        <label class="glass-label">Event Title</label>
                                        {{ form_widget(form.titre_event, {'attr': {'class': 'glass-input', 'placeholder': 'What is the event called?', 'data-min-length': '5', 'required': 'required'}}) }}
                                    </div>

                                    <div class="glass-form-group">
                                        <label class="glass-label">Date & Time</label>
                                        {{ form_widget(form.date_event, {'attr': {'class': 'glass-input flatpickr-input'}}) }}
                                    </div>

                                    <div class="glass-form-group">
                                        <label class="glass-label">Event Type</label>
                                        <div class="custom-select-wrapper">
                                            <select name="{{ form.type_event.vars.full_name }}" id="{{ form.type_event.vars.id }}_dashboard" class="glass-input">
                                                {% for choice in form.type_event.vars.choices %}
                                                    <option value="{{ choice.value }}">{{ choice.value|replace({'_': ' '})|title }}</option>
                                                {% endfor %}
                                            </select>
                                            {% do form.type_event.setRendered %}
                                        </div>
                                    </div>

                                    <div class="glass-form-group">
                                        <label class="glass-label">Location</label>
                                        {{ form_widget(form.lieu_event, {'attr': {'class': 'glass-input', 'placeholder': 'Where?'}}) }}
                                    </div>

                                    <div class="glass-form-group">
                                        <label class="glass-label">Description</label>
                                        {{ form_widget(form.description_event, {'attr': {'class': 'glass-input', 'placeholder': 'Tell us more...', 'rows': '3', 'data-min-length': '10', 'required': 'required'}}) }}
                                    </div>

                                    <div class="row">
                                        <div class="col-md-6">
                                            <div class="glass-form-group">
                                                <label class="glass-label">Total Places</label>
                                                {{ form_widget(form.nb_places, {'attr': {'class': 'glass-input'}}) }}
                                            </div>
                                        </div>
                                        <div class="col-md-6">
                                            <div class="glass-form-group">
                                                <label class="glass-label">Remaining</label>
                                                {{ form_widget(form.nb_restants, {'attr': {'class': 'glass-input'}}) }}
                                            </div>
                                        </div>
                                    </div>

                                    <div class="glass-form-group">
                                        <label class="glass-label">Event Banner</label>
                                        {{ form_widget(form.image_event, {'attr': {'class': 'glass-input'}}) }}
                                    </div>

                                    <button type="submit" class="main-home-btn-auth-submit w-100 mt-4" style="margin: 0; height: 55px;">
                                        Live the Event <i class='bx bx-rocket ms-2'></i>
                                    </button>

                                    {# Hide the default status field which is rendered by form_end automatically #}
                                    {% do form.statut_event.setRendered %}
                                {{ form_end(form) }}
                            </div>
                        </div>
                    </div>

                        {# Pass detailed event data to JS for calendar previews #}
                        <script>
                            window.calendarEvents = {
                                {% for event in evenements %}
                                    "{{ event.dateEvent|date('Y-m-d') }}": {
                                        "id": "{{ event.id }}",
                                        "title": "{{ event.titreEvent }}",
                                        "image": "{{ event.imageEvent ? asset('event_images/' ~ event.imageEvent) : '' }}",
                                        "type": "{{ event.typeEvent }}",
                                        "lieu": "{{ event.lieuEvent }}",
                                        "date": "{{ event.dateEvent|date('F d, Y • H:i') }}"
                                    }{% if not loop.last %},{% endif %}
                                {% endfor %}
                            };
                        </script>

                        </div>
                    </div>
                </div>

               
            </div>

        </div>

        {% include 'frontend/shared/_main_footer.html.twig' %}
        <div style="height: 120px; width: 100%;"></div>
    </div>
</div>

{# Legacy editEventModal removed to favor in-card switcher #}


<input type="hidden" id="currentUserId" value="{{ currentUserId }}">
<input type="hidden" id="currentUserRole" value="{{ currentUserRole }}">

<!-- Custom Delete Confirmation Modal -->
<div id="deleteConfirmModal" class="glass-modal-overlay" onclick="closeGlassModal('deleteConfirmModal')">
    <div class="glass-modal-container" style="max-width: 450px;" onclick="event.stopPropagation()">
        <button type="button" class="glass-modal-close" onclick="closeGlassModal('deleteConfirmModal')">&times;</button>
        
        <div class="glass-modal-body p-0">
            <div style="text-align: center; padding: 3.5rem 2.5rem;">
                <div class="delete-icon-wrapper mb-4" style="width: 80px; height: 80px; background: rgba(255, 77, 77, 0.1); color: #ff4d4d; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 3.5rem; margin: 0 auto;">
                    <i class='bx bxs-error-circle'></i>
                </div>
                <h2 class="text-white fw-bold mb-3" style="font-size: 2rem;">Are you sure?</h2>
                <p style="color: rgba(255,255,255,0.6); font-size: 1.1rem; line-height: 1.6; margin-bottom: 2.5rem;">
                    This action cannot be undone. This event will be permanently removed from Horizon.
                </p>
                <div class="d-flex gap-3">
                    <button type="button" class="btn-event-action" style="background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.1); flex: 1;" onclick="closeGlassModal('deleteConfirmModal')">
                        Cancel
                    </button>
                    <button type="button" id="confirmDeleteBtn" class="btn-event-action" style="background: linear-gradient(135deg, #ff4d4d 0%, #dc2626 100%); color: #fff; border: none; flex: 1.5; font-weight: 700;">
                        Yes, Delete it
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>

{# Participation Modal replaced by in-card switcher #}

{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>
    <script src="{{ asset('frontend/js/form-validation.js') }}"></script>
    <script src="{{ asset('frontend/js/evenement.js') }}"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Initialize validation for Create Event form
            if (document.getElementById('createEventFormDashboard')) {
                const validator = new FormValidator('createEventFormDashboard');
                // Store validator instance globally or on the form element if needed for AJAX error mapping
                document.getElementById('createEventFormDashboard').validator = validator;
            }
             // Initialize validation for Edit Event form
            if (document.getElementById('editEventForm')) {
                const validator = new FormValidator('editEventForm');
                document.getElementById('editEventForm').validator = validator;
            }
        });
    </script>
    <script>
        // Create custom dropdown for select elements
        function createCustomDropdown(selectElement) {
            // Check if already converted
            if (selectElement.parentElement.querySelector('.custom-dropdown-trigger')) {
                return;
            }

            const wrapper = selectElement.closest('.custom-select-wrapper');
            if (!wrapper) return;

            // Get options from select
            const options = Array.from(selectElement.options);
            
            // Create custom dropdown elements
            const trigger = document.createElement('div');
            trigger.className = 'custom-dropdown-trigger';
            trigger.innerHTML = `
                <span class="selected-text">${selectElement.value || selectElement.options[0]?.text || 'Select...'}</span>
                <span class="arrow">&#xea4e;</span>
            `;

            const menu = document.createElement('div');
            menu.className = 'custom-dropdown-menu';

            options.forEach(option => {
                const optionDiv = document.createElement('div');
                optionDiv.className = 'custom-dropdown-option';
                optionDiv.textContent = option.text;
                optionDiv.dataset.value = option.value;
                
                if (option.value === selectElement.value) {
                    optionDiv.classList.add('selected');
                }

                optionDiv.addEventListener('click', () => {
                    // Update select
                    selectElement.value = option.value;
                    
                    // Update UI
                    trigger.querySelector('.selected-text').textContent = option.text;
                    menu.querySelectorAll('.custom-dropdown-option').forEach(opt => {
                        opt.classList.remove('selected');
                    });
                    optionDiv.classList.add('selected');
                    
                    // Close menu
                    trigger.classList.remove('active');
                    menu.classList.remove('active');
                });

                menu.appendChild(optionDiv);
            });

            // Toggle dropdown
            trigger.addEventListener('click', (e) => {
                e.stopPropagation();
                const isActive = trigger.classList.contains('active');
                
                // Close all other dropdowns
                document.querySelectorAll('.custom-dropdown-trigger.active').forEach(t => {
                    t.classList.remove('active');
                    t.nextElementSibling?.classList.remove('active');
                });

                if (!isActive) {
                    trigger.classList.add('active');
                    menu.classList.add('active');
                }
            });

            // Add elements to DOM
            wrapper.appendChild(trigger);
            wrapper.appendChild(menu);

            // Hide native select
            selectElement.style.position = 'absolute';
            selectElement.style.opacity = '0';
            selectElement.style.pointerEvents = 'none';
        }

        // Initialize Custom Form Elements
        function initializeCustomFormElements() {
            // Create custom dropdowns
            const selectElements = document.querySelectorAll('.custom-select-wrapper select');
            selectElements.forEach(select => {
                createCustomDropdown(select);
            });

            // Flatpickr for all datetime inputs
            const dateInputs = document.querySelectorAll('input[name*="date_event"], input.flatpickr-input, input[type="datetime-local"]');
            dateInputs.forEach(input => {
                if (!input._flatpickr) {
                    flatpickr(input, {
                        enableTime: true,
                        dateFormat: "Y-m-d H:i",
                        time_24hr: true,
                        minDate: "today"
                    });
                }
            });

            // Custom Status Selector - FIXED
            const statusSelects = document.querySelectorAll('select[name*="statut_event"]');
            statusSelects.forEach(select => {
                const pillContainer = select.closest('.main-home-form-group');
                if (!pillContainer) return;

                const pills = pillContainer.querySelectorAll('.status-pill');
                if (pills.length === 0) return;

                // Ensure a default value is set if none matches
                let hasActive = false;
                
                // Set initial active state
                pills.forEach(pill => {
                    if (pill.dataset.value === select.value) {
                        pill.classList.add('active');
                        hasActive = true;
                    } else {
                        pill.classList.remove('active');
                    }
                });
                
                // If no value matched, set default to the first pill
                if (!hasActive && pills.length > 0) {
                     const firstPill = pills[0];
                     firstPill.classList.add('active');
                     select.value = firstPill.dataset.value;
                }

                // Add click handlers - using event delegation on container
                const statusContainer = pillContainer.querySelector('.custom-status-selector');
                if (statusContainer && !statusContainer.dataset.initialized) {
                    statusContainer.dataset.initialized = 'true';
                    
                    statusContainer.addEventListener('click', (e) => {
                        const pill = e.target.closest('.status-pill');
                        if (!pill) return;

                        // Remove active from ALL pills
                        pills.forEach(p => p.classList.remove('active'));
                        
                        // Add active to clicked pill
                        pill.classList.add('active');
                        
                        // Update hidden select
                        select.value = pill.dataset.value;
                    });
                }
            });
        }

        // Close dropdowns when clicking outside
        document.addEventListener('click', () => {
            document.querySelectorAll('.custom-dropdown-trigger.active').forEach(trigger => {
                trigger.classList.remove('active');
                trigger.nextElementSibling?.classList.remove('active');
            });
        });

        // Run on page load
        document.addEventListener('DOMContentLoaded', initializeCustomFormElements);

        // Re-initialize when modals are shown
        const modals = document.querySelectorAll('.event-modal');
        modals.forEach(modal => {
            const observer = new MutationObserver(function(mutations) {
                mutations.forEach(function(mutation) {
                    if (mutation.attributeName === 'class' && modal.classList.contains('show')) {
                        setTimeout(initializeCustomFormElements, 100);
                    }
                });
            });
            observer.observe(modal, { attributes: true });
        });

        // Event Grid Pagination - Run after DOM is fully loaded
        document.addEventListener('DOMContentLoaded', function() {
            const eventGrid = document.getElementById('eventGrid');
            if (!eventGrid) return;
            
            const eventsPerPage = 3;
            const allEventCards = Array.from(eventGrid.querySelectorAll('.event-card'));
            const totalPages = Math.ceil(allEventCards.length / eventsPerPage);
            let currentPage = 1;

            const prevBtn = document.getElementById('prevEventPage');
            const nextBtn = document.getElementById('nextEventPage');
            const pagesContainer = document.getElementById('eventPaginationPages');
            const rangeDisplay = document.getElementById('currentEventRange');
            const paginationControls = document.getElementById('eventPaginationControls');

            // Hide pagination if only 1 page or less
            if (totalPages <= 1) {
                if (paginationControls) paginationControls.style.display = 'none';
            }

            function showPage(page) {
                currentPage = page;
                
                // Hide all cards
                allEventCards.forEach(card => {
                    card.style.display = 'none';
                });
                
                // Show cards for current page
                const start = (page - 1) * eventsPerPage;
                const end = start + eventsPerPage;
                allEventCards.slice(start, end).forEach(card => {
                    card.style.display = 'flex';
                });
                
                // Update range display
                if (rangeDisplay) {
                    const showingStart = start + 1;
                    const showingEnd = Math.min(end, allEventCards.length);
                    rangeDisplay.textContent = `${showingStart}-${showingEnd}`;
                }
                
                // Update button states
                if (prevBtn) prevBtn.disabled = page === 1;
                if (nextBtn) nextBtn.disabled = page === totalPages;
                
                // Update page indicators
                renderPageIndicators();
                
                // Scroll to top of grid
                eventGrid.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
            }

            function renderPageIndicators() {
                if (!pagesContainer) return;
                pagesContainer.innerHTML = '';
                for (let i = 1; i <= totalPages; i++) {
                    const pageBtn = document.createElement('button');
                    pageBtn.className = 'event-pagination-page' + (i === currentPage ? ' active' : '');
                    pageBtn.textContent = i;
                    pageBtn.onclick = () => showPage(i);
                    pagesContainer.appendChild(pageBtn);
                }
            }

            if (prevBtn) {
                prevBtn.onclick = () => {
                    if (currentPage > 1) showPage(currentPage - 1);
                };
            }

            if (nextBtn) {
                nextBtn.onclick = () => {
                    if (currentPage < totalPages) showPage(currentPage + 1);
                };
            }

            // Initial render
            showPage(1);
        });
    </script>

    <!-- COOL POPUP SYSTEM INTEGRATION -->
    <div id="coolPopup" class="cool-popup-overlay">
        <div class="cool-popup-content">
            <div id="coolPopupIcon" class="cool-popup-icon"></div>
            <h2 id="coolPopupTitle" class="cool-popup-title"></h2>
            <p id="coolPopupMessage" class="cool-popup-message"></p>
            <button class="cool-popup-btn" onclick="closeCoolPopup()">Got it</button>
        </div>
    </div>

    <style>
        .cool-popup-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.8);
            backdrop-filter: blur(10px);
            display: none;
            justify-content: center;
            align-items: center;
            z-index: 10000;
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .cool-popup-overlay.show {
            display: flex;
            opacity: 1;
        }

        .cool-popup-content {
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            padding: 3rem;
            border-radius: 30px;
            text-align: center;
            max-width: 450px;
            width: 90%;
            transform: scale(0.8);
            transition: transform 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
        }

        .cool-popup-overlay.show .cool-popup-content {
            transform: scale(1);
        }

        .cool-popup-icon {
            font-size: 5rem;
            margin-bottom: 1.5rem;
        }

        .cool-popup-title {
            font-family: 'Outfit', sans-serif;
            font-weight: 800;
            font-size: 2rem;
            margin-bottom: 1rem;
            background: var(--event-gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .cool-popup-message {
            color: rgba(255, 255, 255, 0.7);
            font-size: 1.1rem;
            line-height: 1.6;
            margin-bottom: 2rem;
        }

        .cool-popup-btn {
            background: var(--event-gradient);
            border: none;
            padding: 1rem 2.5rem;
            border-radius: 15px;
            color: white;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 10px 20px -5px rgba(var(--main-home-accent-rgb), 0.5);
        }

        .cool-popup-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 25px -5px rgba(var(--main-home-accent-rgb), 0.6);
        }
    </style>

    <script>
        function showCoolPopup(title, message, type = 'success') {
            const popup = document.getElementById('coolPopup');
            const icon = document.getElementById('coolPopupIcon');
            const titleEl = document.getElementById('coolPopupTitle');
            const msgEl = document.getElementById('coolPopupMessage');

            titleEl.textContent = title;
            msgEl.innerHTML = message;

            if (type === 'success') {
                icon.innerHTML = "<i class='bx bxs-check-circle' style='color: #00d2ff;'></i>";
            } else {
                icon.innerHTML = "<i class='bx bxs-error-circle' style='color: #ff4d4d;'></i>";
            }

            popup.style.display = 'flex';
            setTimeout(() => popup.classList.add('show'), 10);
        }

        function closeCoolPopup() {
            const popup = document.getElementById('coolPopup');
            popup.classList.remove('show');
            setTimeout(() => popup.style.display = 'none', 300);
        }
    </script>
{% endblock %}
