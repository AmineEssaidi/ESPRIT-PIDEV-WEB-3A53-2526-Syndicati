{% extends 'base.html.twig' %}

{% block title %}Dashboard - Liquid Glass Admin{% endblock %}

{% block content %}
{% include 'admin/_dashboard_header.html.twig' with { 'title': 'Dashboard' } %}

<div class="admin-dashboard">
    <!-- Top Stat Cards -->
    <div class="stats-row container row">
        <div class="stat-card col">
            <div class="stat-card-header">
                 <i class="bx bx-male" style="font-size:30px; color: var(--main-home-accent-color);"></i>
                <span class="stat-label">Total number of users</span>
            </div>
            <div class="stat-value">{{ n_utilisateurs }}</div>
            <div class="stat-sub">+12% vs last month</div>
        </div>

        <div class="stat-card col">
            <div class="stat-card-header">
        <i class=" bx bx-building-house" style="font-size:30px; color: var(--main-home-accent-color);"></i>
                <span class="stat-label">Total number of appartments</span>
            </div>
            <div class="stat-value">{{ n_appartements }}</div>
            <div class="stat-sub"></div>
        </div>

        <div class="stat-card col">
            <div class="stat-card-header">
            <i class="bx bx-money" style="font-size:30px; color: var(--main-home-accent-color);"></i>

                <span class="stat-label">Total worth of appartments</span>
            </div>
            <div class="stat-value">{{ appartements_achat|number_format(0, '.', ',') }} TND</div>
            <div class="stat-sub">Amount collected so far</div>
        </div>

        
    </div>


    <div class="dashboard-main">
        <div class="stats-row container row">
        <div class="stat-card col">
            <div class="stat-card-header">
                 <i class="bx bx-send " style="font-size:30px; color: var(--main-home-accent-color);"></i>
                <span class="stat-label">Number of Posts</span>
            </div>
            <div class="stat-value">21</div>
            <div class="stat-sub">+20% vs last month</div>
        </div>
        <div class="stat-card col">
            <div class="stat-card-header">
                 <i class=" bx bx-comment-dots" style="font-size:30px; color: var(--main-home-accent-color);"></i>
                <span class="stat-label">Number of comments</span>
            </div>
            <div class="stat-value">57</div>
            <div class="stat-sub"></div>
        </div>
    </div>
    </div>
      

    
    <div class="row">
        <div class="col">
<!-- Monthly Billing -->
            <section class="billing-card glass-panel">
                <div class="panel-header">
                    <h2 class="panel-title">Monthly billing</h2>
                    <span class="panel-subtitle">Issued vs Received</span>
                </div>
                <div class="billing-content">
                    <div class="billing-gauge">
                        <div class="gauge-ring">
                            <div class="gauge-fill"></div>
                            <div class="gauge-center">
                                <span class="gauge-label">Total</span>
                                <span class="gauge-value">2000</span>
                            </div>
                        </div>
                    </div>
                    <div class="billing-legend">
                        <div class="legend-row">
                            <span class="legend-dot issued-dot"></span>
                            <span>Issued</span>
                            <span class="legend-amount ms-auto">1350 TND</span>
                        </div>
                        <div class="legend-row">
                            <span class="legend-dot received-dot"></span>
                            <span>Received</span>
                            <span class="legend-amount ms-auto">650 TND</span>
                        </div>
                    </div>
                </div>
            </section>

        </div>
        <div class="col">
        <section class="agenda-card glass-panel">
                <div class="panel-header">
                    <h2 class="panel-title">Agenda</h2>
                    <span class="panel-subtitle">This month</span>
                </div>
                <div class="agenda-content">
                    <div class="mini-calendar">
                        <div class="calendar-header">
                            <span class="month-label">February 2026</span>
                        </div>
                        <div class="calendar-grid">
                            <span>Mo</span><span>Tu</span><span>We</span><span>Th</span><span>Fr</span><span>Sa</span><span>Su</span>
                            {% for i in 1..28 %}
                                <button class="calendar-day {{ i == 10 ? 'is-today' : '' }}">{{ i }}</button>
                            {% endfor %}
                        </div>
                    </div>
                    <div class="agenda-list">
                        <div class="agenda-item">
                            <div>
                                <p class="agenda-title">Meeting with Everis</p>
                                <p class="agenda-meta">Zoom · 09:00</p>
                            </div>
                            <span class="agenda-badge">Today</span>
                        </div>
                        <div class="agenda-item">
                            <div>
                                <p class="agenda-title">Board review</p>
                                <p class="agenda-meta">HQ · 15:30</p>
                            </div>
                            <span class="agenda-badge badge-soft">Tomorrow</span>
                        </div>
                    </div>
                </div>
            </section>
            </div>
    </div>
 
    <!-- Main Grid -->
    

        <!-- Right Column -->
        <aside class="dashboard-side">
            <!-- Upcoming Obligations -->
            <section class="obligations-card glass-panel">
                <h2 class="panel-title">Upcoming obligations</h2>
                <div class="obligation-box">
                    <h3 class="obligation-title">VAT declaration</h3>
                    <p class="obligation-text">You have 27 days left to submit the documentation.</p>
                    <button class="btn btn-sm btn-primary rounded-pill">View details</button>
                </div>
            </section>

            <!-- Agenda / Calendar -->
            

         
        </aside>
    </div>
</div>

{% endblock %}

{% block javascripts %}
<script>
// Keep the live date & time in the dashboard header.
// Date/Time logic moved to _dashboard_header.html.twig
</script>
{% endblock %}