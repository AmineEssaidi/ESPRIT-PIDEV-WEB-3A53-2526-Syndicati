{% extends 'base.html.twig' %}


{% block content %}
<div class="container">
    <div class="row">
        <div class="col-sm-8">
        <h3>Appartement {{ appartement.typeA }} à {{ appartement.residence.nomR }}</h3>
            {% if appartement.imageA%}

        <img class="img-fluid rounded" src="{{ appartement.imageA ? asset('images/appartements/' ~  appartement.imageA) : '' }}" width="600"></td>
        </div>
    {% endif %}
    <div class="col-sm-auto">
    <table class="table">
                
            </tr>
        <tbody>

            <tr>
                <th>Type</th>
                <td>{{ appartement.typeA }}</td>
            </tr>
                        <tr>
                <th>Parking</th>
                <td>{{ appartement.parking ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Disponible</th>
                <td>{{ appartement.disponible ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
            <tr>
                <th>Bloc</th>
                <td>{{ appartement.bloc }}</td>
            </tr>
                        <tr>
                <th>Etage</th>
                <td>{{ appartement.etage }}</td>
            </tr>
            <tr>
                <th>Numéro</th>
                <td>{{ appartement.numero }}</td>
            </tr>



                

        </tbody>
    </table>


    <a class="btn btn-info" role="button" href="{{ path('admin_appartement_edit', {'id': appartement.id}) }}">edit</a>

    <a href="{{ include('admin/Residence/_delete_formAppartementBack.html.twig') }}
    </div>
    
    </div>
        <a href="{{ path('admin_appartement') }}" class=" mt-3 btn btn-primary" role="button">Retourner à la liste</a>

</div>
{% endblock %}
{% block javascripts %}
	{{ parent() }}
{% endblock %}