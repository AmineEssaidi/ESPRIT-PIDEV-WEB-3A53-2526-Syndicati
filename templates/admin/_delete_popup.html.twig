<!-- Unified Delete Confirmation Modal -->
<div id="globalDeleteModal" class="global-delete-modal-backdrop">
    <div class="global-delete-modal-glass">
        <div class="global-delete-icon-wrapper">
            <i class="bx bx-trash-alt global-delete-icon"></i>
        </div>
        
        <h3 class="global-delete-title">Delete Item?</h3>
        <p class="global-delete-text">
            This action cannot be undone. <br>
            Are you sure you want to permanently delete this?
        </p>

        <div class="global-delete-actions">
            <button type="button" class="global-delete-btn-cancel" onclick="closeGlobalDeleteModal()">
                Cancel
            </button>
            <button type="button" class="global-delete-btn-confirm" id="globalConfirmDeleteBtn">
                <i class="bx bx-trash"></i> Confirm Delete
            </button>
        </div>
    </div>
</div>

<style>
    /* Global Backdrop */
    .global-delete-modal-backdrop {
        display: none; /* Hidden by default */
        position: fixed;
        top: 0;
        left: 0;
        width: 100vw;
        height: 100vh;
        z-index: 9999;
        background: rgba(0, 0, 0, 0.6);
        backdrop-filter: blur(8px);
        -webkit-backdrop-filter: blur(8px);
        align-items: center;
        justify-content: center;
        opacity: 0;
        transition: opacity 0.3s ease;
    }

    .global-delete-modal-backdrop.active {
        display: flex;
        opacity: 1;
    }

    /* Glass Card - Obsidian Style */
    .global-delete-modal-glass {
        background: #000000; /* Pure Black Obsidian */
        border: 1px solid rgba(255, 255, 255, 0.08); /* Subtle Grey Border */
        box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.9);
        border-radius: 24px;
        padding: 2.5rem;
        width: 90%;
        max-width: 420px;
        text-align: center;
        transform: scale(0.95) translateY(10px);
        transition: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
    }

    .global-delete-modal-backdrop.active .global-delete-modal-glass {
        transform: scale(1) translateY(0);
    }

    /* Animated Icon (Red) */
    .global-delete-icon-wrapper {
        width: 80px;
        height: 80px;
        background: rgba(255, 59, 48, 0.1); /* Red Tint */
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 1.5rem auto;
        border: 1px solid rgba(255, 59, 48, 0.2); /* Red Border */
        box-shadow: 0 0 20px rgba(255, 59, 48, 0.15); /* Red Glow */
        animation: pulse-red 2s infinite;
    }

    .global-delete-icon {
        font-size: 2.5rem;
        color: #ff3b30;
    }

    /* Typography */
    .global-delete-title {
        color: #ffffff;
        font-size: 1.5rem;
        font-weight: 700;
        margin-bottom: 0.5rem;
        letter-spacing: -0.02em;
    }

    .global-delete-text {
        color: rgba(255, 255, 255, 0.6);
        font-size: 0.95rem;
        line-height: 1.5;
        margin-bottom: 2rem;
    }

    /* Actions */
    .global-delete-actions {
        display: flex;
        gap: 1rem;
        justify-content: center;
    }

    .global-delete-btn-cancel {
        background: rgba(255, 255, 255, 0.05);
        color: rgba(255, 255, 255, 0.7);
        border: 1px solid rgba(255, 255, 255, 0.1);
        padding: 0.8rem 1.5rem;
        border-radius: 12px;
        font-weight: 600;
        font-size: 0.95rem;
        cursor: pointer;
        transition: all 0.2s ease;
        flex: 1;
    }

    .global-delete-btn-cancel:hover {
        background: rgba(255, 255, 255, 0.1);
        color: #fff;
    }

    .global-delete-btn-confirm {
        background: rgba(255, 59, 48, 0.15);
        color: #ff3b30;
        border: 1px solid rgba(255, 59, 48, 0.4);
        padding: 0.8rem 1.5rem;
        border-radius: 12px;
        font-weight: 700;
        font-size: 0.95rem;
        cursor: pointer;
        transition: all 0.2s ease;
        flex: 1;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.5rem;
        box-shadow: 0 4px 15px rgba(255, 59, 48, 0.2);
    }

    .global-delete-btn-confirm:hover {
        background: #ff3b30;
        color: #fff;
        border-color: #ff3b30;
        box-shadow: 0 6px 20px rgba(255, 59, 48, 0.4);
        transform: translateY(-2px);
    }

    @keyframes pulse-red {
        0% { box-shadow: 0 0 0 0 rgba(255, 59, 48, 0.4); }
        70% { box-shadow: 0 0 0 15px rgba(255, 59, 48, 0); }
        100% { box-shadow: 0 0 0 0 rgba(255, 59, 48, 0); }
    }
</style>
